{
  "name": "qiankun-vue3",
  "version": "1.0.1",
  "description": "基于qiankun+vue3.0技术栈实现的前端微应用架构。",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:base-vue3 dev:child-*",
    "dev:base-vue3": "pnpm --filter base-vue3 run serve",
    "dev:child-vue2": "pnpm --filter child-vue2 run serve",
    "dev:child-vite-react": "pnpm --filter child-vite-react run serve",
    "dev:child-html": "pnpm --filter child-html run serve",
    "build": "pnpm build:packages && pnpm --recursive --filter \"./main\" --filter \"./child-*\" run build",
    "build:packages": "pnpm --filter \"./packages/*\" run build",
    "build:base-vue3": "pnpm --filter base-vue3 run build",
    "build:child-vue2": "pnpm --filter child-vue2 run build",
    "build:child-vite-react": "pnpm --filter child-vite-react run build",
    "build:child-html": "pnpm --filter child-html run build",
    "clean": "pnpm --recursive exec rm -rf node_modules dist",
    "clean:packages": "pnpm --filter \"./packages/*\" run clean",
    "lint": "pnpm --recursive run lint",
    "type-check": "pnpm --recursive run type-check",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "commit": "cz"
  },
  "keywords": [
    "qiankun",
    "micro-frontend",
    "vue3",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "commitizen": "catalog:",
    "@commitlint/cli": "catalog:",
    "@commitlint/config-conventional": "catalog:"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "lint-staged": {
    "*.{js,jsx,ts,tsx,vue,json,css,scss,md}": "prettier --write"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
